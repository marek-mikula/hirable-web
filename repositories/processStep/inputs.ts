export type StoreData = {
    step: string | null
    isRepeatable: boolean
    triggersAction: string | null
}

export type UpdateData = {
    step: string | null
    isRepeatable: boolean
    triggersAction: string | null
}